{% extends 'base.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}

<div class="container">

    <div class="row my-3">
        <div class="col-12">
            <h2 class="text-muted text-center">Ajouter un nouvel article</h2>
        </div>
        <div class="col-6 mx-auto mt-4">
        {#
    {{ form_start(form, {'attr': }) }} => <form >
    {{ form_end(form) }} => </form>
        #}

            {{form(form, {'attr':{novalidate: 'novalidate'}} ) }}
        </div>
    </div>
</div>


{% endblock %}
